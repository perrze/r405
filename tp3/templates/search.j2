<!DOCTYPE html> 
<html lang="fr"> 
 
{% include 'bootstrap.j2' %} 
 
<body> 
    <header> 
        <div class="mt-4 p-5 bg-success text-white rounded"> 
            <h1>TP 2A - Flask - Exercice 2</h1> 
        </div> 
    </header> 
    <div class="container"> 
        <div class="my-3 p-3 bg-dark text-white rounded"> 
            <h1>Résultat pour la recherche <span class="bg-warning rounded p-1">{{ searched }}</span> 
            </h1> 
            <p>Tableau des {{ numbRes }} résultats dans le champ<span class="bg-warning rounded p-1"> {{ field }} </span></p> 
        </div> 
        {% if found == true%}
        <table class="table">
        {# Ligne de header tableau #}
        <tr>{% for header in fields%}
        <th>{{ header }}</th>
        {%endfor%}
        </tr>
        {# Pour chaque ligne du tableau#}
        {% for line in results %}
        <tr>
        {#Pour chaque contenu#}
        {% for key,cell in line.items() %}
        <td>{{ cell }}</td>
        {% endfor %}
        </tr>
        {%endfor%}
        </table>
        {% endif %}
        
        <a href="/">Retour à l'accueil</a>
</body> 
 
</html> 
 